import { mockedLabs, mockedLaunches, mockedMoons, mockedSubscriptions } from '../space-api/mocks';

export const db = {
  labs: mockedLabs,
  launches: mockedLaunches,
  moons: mockedMoons,
  subscriptions: mockedSubscriptions,
};
